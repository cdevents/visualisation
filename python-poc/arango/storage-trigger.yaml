apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: stroage-trigger
spec:
  broker: default
#  filter:
#    expression: ce.type == "dev.cdevents.pipelinerun.queued.0.1.0"
  subscriber:
    ref:
     apiVersion: v1
     kind: Service
     name: cdevent-storage
